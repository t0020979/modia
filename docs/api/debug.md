# DebugComponent — Компонент отладки

**Версия:** 1.2.0  
**Файл:** `modia/components/debug.js`  
**Тип:** Компонент (автоматическая инициализация)

## 📖 Обзор

`DebugComponent` управляет режимами отладки фреймворка Modia. Автоматически активируется при наличии триггеров (GET-параметры или data-атрибуты) и предоставляет API для проверки состояния отладки.

## 🔌 Подключение

Компонент импортируется автоматически при подключении `modia/index.js`. Явная регистрация не требуется.

```html
<script type="module" src="/modia/index.js"></script>
```

## 📦 API

### Публичный API (через `Modia.debug`)

| Метод | Возвращает | Описание |
|-------|------------|----------|
| `Modia.debug.isEnabled()` | `boolean` | Включён ли debug-режим |
| `Modia.debug.getMode()` | `string \| null` | Текущий режим отладки |
| `Modia.debug.getComponentsRegistry()` | `Array` | Реестр компонентов (для отладки) |

## 🐛 Режимы отладки

| Режим | GET-параметр | Data-атрибут | Описание |
|-------|--------------|--------------|----------|
| `log` | `?modia-log` | `data-modia-log` | Логирование в консоль |
| `debug` | `?modia-debug` | `data-modia-debug` | Debug режим |
| `diag` | `?modia-diag` | `data-modia-diag` | Диагностика (v1.3+) |
| `panel` | `?modia-panel` | `data-modia-panel` | UI панель (v1.3+) |

**Приоритет:** GET-параметры > Data-атрибуты

## 📋 Примеры использования

### Проверка состояния отладки

```javascript
if (Modia.debug.isEnabled()) {
  console.log('Debug активен');
}

const mode = Modia.debug.getMode();
console.log('Режим:', mode); // 'log', 'debug', 'diag', 'panel', или null
```

### Получение реестра компонентов

```javascript
const registry = Modia.debug.getComponentsRegistry();
registry.forEach(comp => {
  console.log(comp.constructor.name, 'на', comp.$el[0]);
});
```

### Включение режима через JavaScript

```javascript
// До загрузки Modia
$('body').attr('data-modia-debug', '1');

// Или через GET-параметр в URL
// ?modia-debug
```

## 🔍 Диагностика

### Проверка активации

```javascript
// На странице без триггеров
Modia.debug.isEnabled(); // false
Modia.debug.getMode();   // null

// На странице с ?modia-debug
Modia.debug.isEnabled(); // true
Modia.debug.getMode();   // 'debug'
```

### Реестр компонентов

```javascript
// После инициализации
const count = Modia.debug.getComponentsRegistry().length;
console.log('Всего компонентов:', count);
```

⚠️ **Важно:** Реестр предназначен только для отладки, не используйте в бизнес-логике.

## 📊 Примеры тестирования

### Тест без режима

```
URL: test-debug.html
Ожидаемо: isEnabled() = false, getMode() = null
```

### Тест с GET-параметром

```
URL: test-debug.html?modia-debug
Ожидаемо: isEnabled() = true, getMode() = 'debug'
```

### Тест с data-атрибутом

```html
<body data-modia-log>
```

```
Ожидаемо: isEnabled() = true, getMode() = 'log'
```

## ⚙️ Планируемые возможности (v1.3+)

| Функция | Описание | Статус |
|---------|----------|--------|
| UI панель | Красивый вывод логов в DOM | ⏳ v1.3 |
| Диагностика страницы | Анализ подключённых компонентов | ⏳ v1.3 |
| Экспорт логов | Скачивание JSON с логами | ⏳ v1.4 |
| Блокировка ENV | `window.MODIA_DEBUG_ENABLED` | ⏳ v1.3 |

## 🚫 Ограничения

| Ограничение | Описание |
|-------------|----------|
| Нет UI панели | Только консольное логирование (v1.2) |
| Нет истории логов | Логи не сохраняются (v1.3+) |
| Нет экспорта | Скачивание логов (v1.4+) |

## 📚 Связанная документация

| Файл | Описание |
|------|----------|
| [logger.md](./logger.md) | Logger — сервис логирования |
| [index.md](./index.md) | Index — точка входа фреймворка |
| [core.md](./core.md) | Core — базовые классы |

---

**Последнее обновление:** 2026-02-17  
**Статус:** ✅ Реализовано (v1.2)
