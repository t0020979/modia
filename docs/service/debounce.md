# Debounce Service

## 📖 Обзор

Сервис `debounce` ограничивает частоту вызова функции, откладывая её выполнение до тех пор, пока не пройдёт указанное время без новых вызовов.

**Основное назначение:** оптимизация live-валидации и других операций, которые не должны выполняться при каждом событии (например, при вводе текста в поле).

## 🔌 Подключение

```javascript
import { debounce } from '../modia/services/debounce.js';
```

Сервис не требует дополнительных зависимостей и не загрязняет глобальную область видимости.

## 📦 Базовое использование

### Отложенный вызов (по умолчанию)

Функция вызывается только после паузы в вызовах:

```javascript
const saveHandler = debounce(() => {
  console.log('Сохранение данных...');
}, 300);

// При быстрых вызовах — функция выполнится только 1 раз
saveHandler();
saveHandler();
saveHandler();
// → 'Сохранение данных...' через 300мс после последнего вызова
```

### Немедленный вызов (immediate)

Функция вызывается сразу при первом вызове, затем игнорируется до конца паузы:

```javascript
const clickHandler = debounce(() => {
  console.log('Клик обработан');
}, 300, true);

// Первый вызов — немедленно
clickHandler();  // → 'Клик обработан' сразу

// Повторные вызовы в паузе — игнорируются
clickHandler();  // → ничего
clickHandler();  // → ничего

// После паузы — снова можно вызвать
setTimeout(() => {
  clickHandler();  // → 'Клик обработан' снова
}, 400);
```

## 📋 Примеры использования

### Пример 1: Live-валидация поля

```javascript
// Внутри ValidationComponent
import { debounce } from '../modia/services/debounce.js';

const validateField = debounce(($field) => {
  const validator = new FieldValidator($field);
  validator.validate();
}, 300);

$field.on('input', () => {
  validateField($field);
});
```

### Пример 2: Поиск с автодополнением

```javascript
const searchHandler = debounce((query) => {
  fetch(`/api/search?q=${query}`)
    .then(response => response.json())
    .then(data => renderResults(data));
}, 500);

$('#search-input').on('input', function() {
  searchHandler($(this).val());
});
```

### Пример 3: Обработка изменения размера окна

```javascript
const resizeHandler = debounce(() => {
  console.log('Размер окна:', window.innerWidth);
  recalculateLayout();
}, 200);

$(window).on('resize', resizeHandler);
```

## ⚙️ Параметры

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `func` | Function | — | Функция для отложенного вызова |
| `wait` | number | 300 | Время ожидания в миллисекундах |
| `immediate` | boolean | false | Если true — вызвать немедленно при первом вызове |

## 🚫 Ограничения

| Ограничение | Описание |
|-------------|----------|
| Нет `cancel()` | В текущей версии нельзя отменить запланированный вызов |
| Нет `flush()` | Нельзя принудительно выполнить отложенный вызов |
| Ошибки в `func` | Логгируются в консоль, но не прерывают работу таймера |

## 💡 Рекомендации

| Сценарий | Рекомендуемая задержка |
|----------|----------------------|
| Live-валидация | 300мс |
| Поиск с автодополнением | 400-500мс |
| Изменение размера окна | 150-200мс |
| Прокрутка страницы | 100-150мс |

## 📚 Связанная документация

| Файл | Описание |
|------|----------|
| [`api/service/debounce.md`](../../api/service/debounce.md) | API Reference (параметры, методы) |
| [`docs/service/logger.md`](../logger.md) | Logger Service |
| [`docs/components/validation.md`](../components/validation.md) | Validation Component |
