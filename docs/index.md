# 🚪 Entry Point — `index.js`

> Точка входа Modia. Отвечает за **регистрацию компонентов**, **автоматическую инициализацию** и **экспорт глобального API**.  
> Не содержит бизнес-логики. Не зависит от конкретных компонентов, кроме как через импорт и регистрацию.

## 🎯 Назначение

1. **Регистрация всех компонентов** фреймворка через `ComponentScanner.register()`.
2. **Автоматический запуск** сканирования при загрузке страницы (`$(document).ready`).
3. **Поддержка Turbolinks** — повторное сканирование при `turbolinks:load`.
4. **Экспорт глобального интерфейса** `window.Modia` для ручного использования (AJAX, динамический DOM).

Этот файл — **единственный**, кто знает о существовании конкретных компонентов.  
Все остальные модули (`core.js`, `BaseComponent`) — полностью обобщены.

## 🔧 Публичный интерфейс

```js
window.Modia = {
  scan(root = document) {
    return ComponentScanner.scan(root);
  },
  ComponentScanner // для расширенного использования (редко)
};
```

> **Использование**:
> ```js
> // После AJAX-вставки
> $('#modal').html(html);
> Modia.scan($('#modal')[0]);
> ```

## 🔄 Жизненный цикл

| Этап | Действие |
|------|--------|
| Импорт | Все компоненты импортируются и регистрируются один раз |
| DOM ready | `ComponentScanner.scan()` → создаются все компоненты на странице |
| Turbolinks | При `turbolinks:load` — повторное сканирование всего `document` |
| Динамика | Разработчик вызывает `Modia.scan(container)` после вставки нового HTML |

> **Важно**: сканирование **не дублирует** уже инициализированные компоненты,  
> так как компонент привязан к DOM-элементу, а повторная инициализация не отслеживается.  
> Рекомендуется: удалять старый DOM перед вставкой нового (стандартное поведение Rails UJS).

## 📦 Зависимости

- **Обязательные**:
  - `core.js` → `ComponentScanner`
  - Все компоненты (`components/*.js`)
  - jQuery → `$(document).ready`

- **Опциональные**:
  - `window.Turbolinks` → поддержка `turbolinks:load`

> Нет прямой зависимости от `Container` — его создание и использование остаётся на усмотрение компонентов и приложения.

## 🛠 Расширяемость

Чтобы добавить новый компонент:
1. Создай файл в `components/`.
2. Импортируй его в `index.js`.
3. Зарегистрируй: `ComponentScanner.register(MyComponent);`

```js
// Пример добавления компонента
import { MyComponent } from './components/my_component.js';
ComponentScanner.register(MyComponent);
```

> **Запрещено**:
> - Добавлять логику инициализации вне регистрации.
> - Вызывать `new MyComponent()` вручную.
> - Модифицировать DOM или состояние в `index.js`.

## ✅ Критерии проверки соответствия

- [ ] Все компоненты зарегистрированы через `ComponentScanner.register()`.
- [ ] Автоматическая инициализация работает при `$(document).ready`.
- [ ] Поддержка Turbolinks реализована через слушатель события.
- [ ] Глобальный `Modia.scan()` доступен и работает с любым `root`.
- [ ] Файл не содержит логики компонентов, только импорты и регистрацию.

> Этот модуль — **точка сборки фреймворка**. Любые изменения должны сохранять его пассивность и декларативность.
