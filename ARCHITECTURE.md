# 🏗️ Архитектура Modia

Лёгкий компонентный фреймворк на чистом JavaScript для управления динамическим UI в legacy-проектах. Без внешних зависимостей. Совместим с Rails, jQuery, Webpacker.

## 📖 Оглавление
- [Ключевые принципы](#-ключевые-принципы)
- [Архитектурные слои](#-архитектурные-слои)
- [Поток данных](#-поток-данных)
- [Жизненный цикл компонента](#-жизненный-цикл-компонента)
- [Обработка ошибок](#-обработка-ошибок)
- [Стратегия тестирования](#-стратегия-тестирования)
- [Что НЕ является частью архитектуры](#-что-не-является-частью-архитектуры)
- [Документация](#-документация)

---

## 🧱 Ключевые принципы

| Принцип | Описание |
|---------|----------|
| **Автоинициализация** | Компоненты создаются автоматически при наличии `data-component` |
| **Декларативная конфигурация** | Настройка через `data-*` атрибуты без JavaScript |
| **Изоляция** | Каждый компонент работает только внутри своего корня (`this.$el`) |
| **Расширяемость без изменения ядра** | Новые правила → массив, новые режимы → мета-атрибуты |
| **Поддержка динамики** | `Modia.scan(container)` для AJAX/Turbolinks |
| **Поведение контейнера** | Компонент на форме/секции, не на отдельных полях |

---

## 📦 Архитектурные слои

```
modia/
├── core.js              ← Ядро: BaseComponent, Container, ComponentScanner
├── index.js             ← Точка входа: регистрация + экспорт Modia.scan()
├── modia.js             ← Алиас для совместимости (импортирует index.js)
├── components/          ← Компоненты (поведение контейнера)
├── services/            ← Сервисы (логика без состояния)
│   ├── logger.js        ← ✅ v1.2: Единый сервис логирования
│   └── debug.js         ← ✅ v1.2: Компонент отладки
└── configurations/      ← Конфигурации (чистые данные)
```

### Ядро (`core.js`)

| Класс | Ответственность |
|-------|-----------------|
| `BaseComponent` | Базовый класс с парсингом `data-component-*` → `this.config` |
| `Container` | Управление глобальным состоянием и рассылка `onStateChange()` |
| `ComponentScanner` | Автоматическое обнаружение и создание компонентов |

### Компоненты

**Поведение контейнера, а не поля:**

```html
<form data-component="validation">   ← компонент на форме
  <input required>                   ← поле НЕ является компонентом
</form>
```

### Сервисы

Чистая логика без состояния:

| Сервис | Ответственность |
|--------|-----------------|
| `FieldValidator` | Валидация одного поля по правилам |
| `FieldErrorRenderer` | Только отображение/очистка ошибок |
| `debounce` | Утилита для оптимизации `live`-валидации |

### Конфигурации

Чистые данные без логики:

```javascript
export const validationRules = [
  {
    name: 'required',
    selector: '[required]',
    validate($field, validator) { ... },
    getTemplateId() { return 'error_span'; },
    fallbackMessage() { ... }
  }
];
```

---

## 🔄 Поток данных

```
┌─────────┐     ┌──────────────────┐     ┌─────────────┐     ┌──────────────┐
│   DOM   │ ──→ │ ComponentScanner │ ──→ │  Component  │ ──→ │  Container   │
│ (HTML)  │     │   (scan root)    │     │ (create)    │     │ (state mgr)  │
└─────────┘     └──────────────────┘     └─────────────┘     └──────────────┘
                                                              │
                                                              ↓
                                                      onStateChange()
                                                              │
                                                              ↓
                                                     ┌─────────────┐
                                                     │   Service   │
                                                     │ (Validator) │
                                                     └─────────────┘
                                                              │
                                                              ↓
                                                     ┌─────────────┐
                                                     │  Renderer   │
                                                     │  (Error)    │
                                                     └─────────────┘
```

**Последовательность:**
1. `ComponentScanner.scan(root)` находит `[data-component="name"]`
2. Создаётся экземпляр компонента → парсинг конфигурации
3. Компонент регистрируется в `Container` (опционально)
4. Компонент слушает события, обновляет UI
5. При `Container.setState()` → `onStateChange()` у всех компонентов

---

## 🔁 Жизненный цикл компонента

| Стадия | Метод | Описание |
|--------|-------|----------|
| **Сканирование** | `ComponentScanner.scan()` | Находит `[data-component="name"]` в DOM |
| **Создание** | `new Component(element)` | Парсинг конфигурации из `data-*` |
| **Регистрация** | `container.addComponent()` | Добавляет компонент в контейнер (опционально) |
| **Работа** | `onStateChange(state)` | Слушает события, обновляет UI |
| **Уничтожение** | `destroy()` | Очищает обработчики при замене DOM |

### Обработка AJAX/Turbolinks

```javascript
// После вставки нового HTML
$('#modal').html(newHtml);
Modia.scan($('#modal')[0]); // инициализировать компоненты внутри

// При уничтожении (если нужно)
component.destroy(); // очищает event listeners
```

### Обработка замены DOM

При замене контейнера через AJAX:
1. Вызвать `destroy()` у старых компонентов (если есть ссылка)
2. Вызвать `Modia.scan(newContainer)` для новых
3. Компоненты автоматически создадутся заново

---

## ⚠️ Обработка ошибок

### Архитектура обработки ошибок

```
┌─────────────────────┐     ┌───────────────────┐     ┌───────────────────┐
│  ValidationComponent│ ──→ │ FieldValidator    │ ──→ │ FieldErrorRenderer│
│  (orchestrator)     │     │ (validation logic)│     │ (DOM manipulation)│
└─────────────────────┘     └───────────────────┘     └───────────────────┘
         │                        │                          │
         │                        ↓                          ↓
         │              ┌──────────────────┐     ┌─────────────────┐
         └────────────→ │ validationRules  │     │ error-templates │
                        │ (config)         │     │ (DOM)           │
                        └──────────────────┘     └─────────────────┘
```

### Иерархия источников ошибок (5 уровней)

| Приоритет | Источник | Пример |
|-----------|----------|--------|
| 1 | Inline-атрибут | `data-error-text-required="Заполните"` |
| 2 | Шаблон в контейнере | `.error-templates [data-rule="required"]` |
| 3 | Отдельные шаблоны | `#error_span`, `#format-error-span` |
| 4 | Сообщение из правила | `rule.defaultMessage` |
| 5 | Fallback | `"Field error"` (только для отладки) |

### Propagation ошибок

1. `FieldValidator.validate()` → возвращает `{ isValid, errors: [] }`
2. `ValidationComponent` получает результат
3. `FieldErrorRenderer.render()` → отображает ошибки
4. `FieldErrorRenderer.clear()` → очищает при успехе

---

## 🧪 Стратегия тестирования

### Уровни тестирования

| Уровень | Расположение | Инструмент |
|---------|--------------|------------|
| **Unit-тесты** | `tests/unit/` | Jest |
| **Integration-тесты** | `tests/integration/` | Jest + jsdom |
| **E2E-примеры** | `examples/` | Браузер |

### Изоляция компонентов

```javascript
// Unit-тест для FieldValidator
describe('FieldValidator', () => {
  it('validates required field', () => {
    const validator = new FieldValidator($field, $root, rules, renderer);
    expect(validator.validate()).toEqual({ isValid: false, errors: [...] });
  });
});
```

### Тестирование с DOM

```javascript
// Integration-тест с jsdom
describe('ValidationComponent', () => {
  beforeEach(() => {
    document.body.innerHTML = `
      <form data-component="validation">
        <input name="email" required>
        <div class="error-templates">
          <span data-rule="required">Required</span>
        </div>
      </form>
    `;
  });

  it('renders error on submit', () => {
    Modia.scan();
    document.querySelector('form').dispatchEvent(new Event('submit'));
    expect(document.querySelector('.text-danger')).toBeInTheDocument();
  });
});
```

---

## 🚫 Что НЕ является частью архитектуры

| Запрещено | Почему |
|-----------|--------|
| Компонент на поле (`<input data-component="...">`) | Нарушает принцип «поведение контейнера» |
| Смешивание рендеринга и логики в одном классе | Нарушает разделение ответственности |
| Глобальные переменные вне `window.Modia` | Загрязняет глобальную область |
| Изменение ядра для нового правила | Нарушает расширяемость |
| Прямая манипуляция DOM из компонента | Используйте сервисы-рендереры |

---

## 📚 Документация

| Аудитория | Файлы |
|-----------|-------|
| **Пользователи** | `README.md`, `docs/api/` |
| **Разработчики фреймворка** | `CONTRIBUTING.md`, `docs/components/` |
| **Архитектор** | Этот файл + `contracts/` (внутренние) |

---

> **Modia** — от modular + via  
> 💡 Шаг к системной организации JS в legacy-проектах без миграции на React/Vue.

**Последнее обновление:** 2026-02-16
