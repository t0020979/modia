# 🗺 modia_roadmap.md — План развития фреймворка Modia

> Этот документ описывает **эволюцию Modia** как системы:  
> — текущие возможности,  
> — запланированные архитектурные слои,  
> — компоненты к реализации,  
> — поддержку иерархии и событий.  
>  
> Он **не дублирует README**, а служит **дорожной картой для разработчика фреймворка**.

---

## 🧱 Текущее ядро (v1.0 — реализовано)

| Модуль | Ответственность |
|-------|-----------------|
| `BaseComponent` | Базовый класс: конфигурация, изоляция, `onStateChange` |
| `Container` | Глобальное состояние → рассылка **всем компонентам** |
| `ComponentScanner` | Авто-инициализация по `data-component` |
| `ValidationComponent` | Валидация формы (submit + live), через `FieldValidator` и `FieldErrorRenderer` |

> ✅ Работает в legacy-проектах, совместим с Rails, Turbolinks, UJS.

---

## 🚧 Ближайшие архитектурные расширения (v1.1 — в работе)

### 1. **Глобальный Event Bus**
- **Почему**: `Container.setState()` слишком грубый — не позволяет передавать данные, только состояние.
- **Решение**: добавить `EventBus` как singleton.
  ```js
  EventBus.emit('form:validated', { isValid: false, errors: [...] });
  EventBus.on('modal:opened', callback);
  ```
- **Совместимость**: остаётся опциональным — компоненты могут использовать `Container`, `EventBus` или оба.

### 2. **Поддержка вложенных компонентов**
- **Проблема**: сейчас все компоненты — равноправные, нет иерархии.
- **Решение**:
  - При сканировании: рекурсивно инициализировать **внутри корня компонента**.
  - Ввести **локальный контейнер** (`LocalContainer`) для дочерних компонентов.
  - Поддержка: `data-component-scope="local"` → компонент создаёт свой `Container`.

> Пример:  
> ```html
> <div data-component="modal">
>   <form data-component="validation">...</form> <!-- дочерний -->
> </div>
> ```
> → `ValidationComponent` получает **локальный state** от `ModalComponent`, а не глобальный.

---

## 🧩 Запланированные компоненты (по приоритету)

| Компонент | Назначение | Состояние |
|----------|-----------|----------|
| `FormStateComponent` | Переключение режимов (`regular`/`cross`) через `Container` | v1.2 |
| `ModalComponent` | Открытие/закрытие, сканирование внутреннего DOM, локальный контейнер | v1.2 |
| `DynamicSelectComponent` | Асинхронная подгрузка опций в `<select>` | v1.3 |
| `DependentFieldComponent` | Блокировка/очистка полей при изменении другого | v1.3 |
| `TabGroupComponent` | Переключение вкладок с поддержкой `data-component` внутри | v1.4 |
| `AccordionComponent` | Сворачиваемые секции с сохранением состояния | v1.4 |
| `CounterComponent` | Счётчик длины, выбранного и т.п. | v1.5 |
| `FormLockerComponent` | Блокировка после отправки (`data-disable-with`) | v1.5 |

> Все компоненты следуют принципу: **поведение контейнера, а не поля**.

---

## 📦 Архитектурные слои (на горизонте)

| Слой | Описание |
|------|--------|
| **Core Layer** | `BaseComponent`, `Container`, `EventBus`, `ComponentScanner` |
| **Service Layer** | `FieldValidator`, `FieldErrorRenderer`, `StateResolver` |
| **Component Layer** | `ValidationComponent`, `ModalComponent`, и др. |
| **Configuration Layer** | `validationRules.js`, `formStateMap.js`, `templateSources.js` |
| **Integration Layer** | Совместимость: Turbolinks, UJS, Rails i18n, Webpacker |

---

## 🔄 Версионная стратегия

- **v1.x** — базовая архитектура + компоненты, **без breaking changes**.
- **v2.0** — возможна:
  - поддержка вложенных контейнеров,
  - унификация источников ошибок через `TemplateRegistry`,
  - опциональный отказ от jQuery (чистый DOM API).

> **Принцип**: даже временные решения в v1.x должны соответствовать архитектуре — чтобы v2.0 не требовал переписывания.

---

## 📅 Принципы развития

- **Каждый компонент = контракт**: требования, ограничения, критерии.
- **Новые возможности** — через расширение, **не через переопределение**.
- **Совместимость с legacy** — обязательна: не ломать DOM, не мешать UJS.
- **Тестируемость** — заложена в архитектуру: чистые функции, изоляция, абстракции.

> Этот документ — **живой**. Обновляется при каждом архитектурном решении.


## 📜 История релизов

| Версия | Дата | Ключевые изменения |
|--------|------|-------------------|
| v1.0 | 2026-01-15 | Базовое ядро: BaseComponent, Container, ValidationComponent |
| v1.1 | 2026-02-16 | Вложенные компоненты, расширенные правила валидации |
| **v1.2** | **2026-02-17** | **✅ Logger Service, DebugComponent, jQuery Data API, кастомные события** |
| v1.3 | 2026-04-01 (план) | EventBus, DynamicSelectComponent, DependentFieldComponent, UI панель отладки |


