# Logger Service — Единый сервис логирования

**Версия:** 1.2.0  
**Файл:** `modia/services/logger.js`  
**Тип:** Singleton сервис

## 📖 Обзор

`Logger` — централизованный сервис для логирования событий фреймворка Modia. Позволяет включать/выключать логирование через GET-параметры или data-атрибуты, что удобно для разработки и отладки.

## 🔌 Подключение

Logger импортируется автоматически при подключении `modia/index.js`.

```html
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<script type="module" src="/modia/index.js"></script>
```

## Глобальные переменные

| Переменная | Тип | Описание |
|------------|-----|----------|
| `window.MODIA_LOG_ENABLED` | boolean | `false` = полная блокировка всех логов |
| `window.MODIA_LOG_LEVEL` | string | `'info'` \| `'warn'` \| `'error'` — минимальный уровень |

## Иерархия активации

| Приоритет | Триггер | Описание |
|-----------|---------|----------|
| 1 (высший) | ENV-переменные | Блокирует всё остальное |
| 2 | GET-параметры | ?modia-log, ?modia-debug |
| 3 (низший) | Data-атрибуты | [data-modia-log] |

## 📦 API

### Через глобальный объект `Modia`

После инициализации фреймворка доступен через `window.Modia`:

| Метод | Описание |
|-------|----------|
| `Modia.log(message, level, context)` | Основное API логирования |
| `Modia.info(message, context)` | Лог уровня info |
| `Modia.warn(message, context)` | Лог уровня warn |
| `Modia.error(message, context)` | Лог уровня error |
| `Modia.success(message, context)` | Лог уровня success |

### Прямой импорт (для внутренних модулей)

```javascript
import { logger } from '../services/logger.js';

logger.info('Сообщение', 'ContextName');
logger.warn('Предупреждение', 'ContextName');
logger.error('Ошибка', 'ContextName');
logger.success('Успех', 'ContextName');
```

## 🐛 Режимы отладки

Logger активируется только при наличии одного из триггеров:

| Триггер | Пример | Приоритет |
|---------|--------|-----------|
| GET-параметр | `?modia-log` | 1 (наивысший) |
| GET-параметр | `?modia-debug` | 1 |
| GET-параметр | `?modia-diag` | 1 |
| GET-параметр | `?modia-panel` | 1 |
| Data-атрибут | `<div data-modia-log>` | 2 |
| Data-атрибут | `<div data-modia-debug>` | 2 |
| Data-атрибут | `<div data-modia-diag>` | 2 |
| Data-атрибут | `<div data-modia-panel>` | 2 |

**Важно:** GET-параметры работают без значения (`?modia-log`), значение не требуется.

## 📋 Примеры использования

### Базовое логирование

```javascript
Modia.log('Обычное сообщение', 'info', 'MyComponent');
Modia.info('Информация', 'MyComponent');
Modia.warn('Предупреждение', 'MyComponent');
Modia.error('Ошибка', 'MyComponent');
Modia.success('Успех', 'MyComponent');
```

### Вывод в консоли

При активном режиме отладки:

```
[12:34:56] [Modia] MyComponent Информационное сообщение
[12:34:57] [Modia] MyComponent ⚠️ Предупреждение
[12:34:58] [Modia] MyComponent ❌ Ошибка
[12:34:59] [Modia] MyComponent ✅ Успех
```

Без активного режима — консоль молчит.

### Для разработчиков компонентов

```javascript
// В своём компоненте
import { logger } from '../services/logger.js';

class MyComponent {
  constructor(element) {
    logger.info('Компонент создан', 'MyComponent');
  }
  
  destroy() {
    logger.info('Компонент уничтожен', 'MyComponent');
  }
}
```

## ⚙️ Конфигурация (v1.3+)

Планируется поддержка ENV-переменных для продакшена:

```javascript
window.MODIA_LOG_ENABLED = false;  // Полная блокировка
window.MODIA_LOG_LEVEL = 'error';  // Только ошибки
```

## 📊 Уровни логирования

| Уровень | Метод | Console метод | Описание |
|---------|-------|---------------|----------|
| `info` | `logger.info()` | `console.log()` | Информационные сообщения |
| `warn` | `logger.warn()` | `console.warn()` | Предупреждения |
| `error` | `logger.error()` | `console.error()` | Ошибки |
| `success` | `logger.success()` | `console.log()` | Успешные операции |
| `log` | `logger.log()` | `console.log()` | Обычные сообщения (по умолчанию) |

## 🚫 Ограничения

| Ограничение | Описание |
|-------------|----------|
| Нет истории логов | Логи не сохраняются (v1.3+) |
| Нет UI панели | Только консоль (v1.3+) |
| Нет remote logging | Отправка на сервер не поддерживается (v1.4+) |
| Нет блокировки ENV | Блокировка через переменные (v1.3+) |

## 📚 Связанная документация

| Файл | Описание |
|------|----------|
| [debug.md](./debug.md) | DebugComponent — управление режимами отладки |
| [index.md](./index.md) | Index — точка входа фреймворка |
| [core.md](./core.md) | Core — базовые классы |

---

**Последнее обновление:** 2026-02-17  
**Статус:** ✅ Реализовано (v1.2)