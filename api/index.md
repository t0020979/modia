# Index — Точка входа фреймворка

**Версия:** 1.2.0  
**Файл:** `modia/index.js`  
**Тип:** Entry point

## 📖 Обзор

`index.js` — точка входа фреймворка Modia. Отвечает за:

- Регистрацию компонентов в `ComponentScanner`
- Экспорт публичного API через `window.Modia`
- Автоматическую инициализацию при загрузке страницы
- Интеграцию с Logger и DebugComponent

## 🔌 Подключение

### Вариант 1: Через `<script>` с module

```html
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<script type="module" src="/modia/index.js"></script>
```

### Вариант 2: Через импорт в вашем JS

```javascript
import '/modia/index.js';

// После загрузки доступен window.Modia
console.log(Modia.version); // '1.2.0'
```

## 📦 Публичный API

### `window.Modia`

Глобальный объект фреймворка.

| Свойство/Метод | Тип | Описание |
|----------------|-----|----------|
| `ComponentScanner` | Class | Класс сканера компонентов |
| `scan(root)` | Method | Ручное сканирование контейнера |
| `version` | String | Версия фреймворка |
| `log(message, level, context)` | Method | Логирование (через Logger) |
| `info(message, context)` | Method | Лог уровня info |
| `warn(message, context)` | Method | Лог уровня warn |
| `error(message, context)` | Method | Лог уровня error |
| `success(message, context)` | Method | Лог уровня success |
| `debug.isEnabled()` | Method | Проверка активности debug |
| `debug.getMode()` | Method | Получение текущего режима |
| `debug.getComponentsRegistry()` | Method | Реестр компонентов |

---

### `Modia.scan(root)`

Ручное сканирование контейнера на наличие компонентов.

**Параметры:**

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `root` | HTMLElement\|Document | `document` | Корневой элемент для сканирования |

**Возвращает:** `Array` — массив созданных экземпляров компонентов.

**Пример:**

```javascript
// После вставки HTML через AJAX
$('#modal').html(newHtml);
Modia.scan($('#modal')[0]);
```

---

### `Modia.version`

Строка с версией фреймворка.

**Пример:**

```javascript
console.log(Modia.version); // '1.2.0'
```

---

### `Modia.log()` и уровни

Единый API логирования (через Logger Service).

**Пример:**

```javascript
Modia.log('Сообщение', 'info', 'Context');
Modia.info('Информация', 'Context');
Modia.warn('Предупреждение', 'Context');
Modia.error('Ошибка', 'Context');
Modia.success('Успех', 'Context');
```

⚠️ **Важно:** Логи выводятся только при активном режиме отладки (`?modia-log`, `?modia-debug`, или data-атрибут).

---

### `Modia.debug.*`

API для управления режимами отладки (через DebugComponent).

**Пример:**

```javascript
if (Modia.debug.isEnabled()) {
  console.log('Debug активен');
}

const mode = Modia.debug.getMode(); // 'log', 'debug', 'diag', 'panel', или null
const registry = Modia.debug.getComponentsRegistry(); // массив компонентов
```

---

## 🚀 Автоинициализация

Modia автоматически инициализируется при:

1. **DOMContentLoaded** — стандартная загрузка страницы

**Защита от дублирования:**

```javascript
window.__modia_initialized // флаг защиты
```

### Кастомные события

```javascript
// После инициализации
$(document).on('modia:initialized', (e, data) => {
  console.log('Инициализировано компонентов:', data.instances.length);
});

// После сканирования
$(document).on('modia:scanned', (e, data) => {
  console.log('Найдено компонентов:', data.instances.length);
});

// После создания компонента
$(document).on('modia:component-created', (e, data) => {
  console.log('Создан компонент:', data.component.constructor.name);
});
```

---

## 🐛 Debug-режимы

### Способы включения

| Способ | Пример | Приоритет |
|--------|--------|-----------|
| GET-параметр | `?modia-log` | 1 (наивысший) |
| GET-параметр | `?modia-debug` | 1 |
| GET-параметр | `?modia-diag` | 1 |
| GET-параметр | `?modia-panel` | 1 |
| Data-атрибут | `<div data-modia-log>` | 2 |
| Data-атрибут | `<div data-modia-debug>` | 2 |

### Примеры

**Через URL:**

```
https://example.com/form?modia-debug
https://example.com/form?modia-log
```

**Через атрибут:**

```html
<body data-modia-debug>
  <!-- Контент -->
</body>
```

---

## 📋 Примеры использования

### Базовое использование

```html
<!DOCTYPE html>
<html>
<head>
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script type="module" src="/modia/index.js"></script>
</head>
<body>
  <form data-component="validation">
    <input type="email" name="email" required>
    <button type="submit">Отправить</button>
  </form>
</body>
</html>
```

### Сканирование после AJAX

```javascript
$.get('/form-partial', (html) => {
  $('#container').html(html);
  Modia.scan($('#container')[0]);
});
```

### Проверка версии

```javascript
if (Modia.version >= '1.2.0') {
  // Использовать новые возможности
}
```

### Логирование

```javascript
Modia.info('Форма отправлена', 'FormComponent');
Modia.error('Ошибка валидации', 'ValidationComponent');
```

---

## ⚠️ Известные ограничения

| Ограничение | Описание | План |
|-------------|----------|------|
| jQuery требуется | Без jQuery 3.5.1+ фреймворк не работает | v2.0: опциональный отказ |
| Turbolinks отключён | Поддержка отложена | v1.3: полная интеграция |
| Нет ENV блокировки | Блокировка логов через переменные | v1.3: `window.MODIA_LOG_ENABLED` |

---

## 📚 Связанная документация

| Файл | Описание |
|------|----------|
| [core.md](./core.md) | Core — базовые классы |
| [logger.md](./logger.md) | Logger — сервис логирования |
| [debug.md](./debug.md) | DebugComponent — режимы отладки |
| [ARCHITECTURE.md](../ARCHITECTURE.md) | Архитектурные принципы |

---

**Последнее обновление:** 2026-02-17  
**Статус:** ✅ Реализовано (v1.2)
