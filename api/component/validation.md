# ValidationComponent — API Reference

**Версия:** 1.2.0  
**Последнее обновление:** 2026-02-25

---

## 📦 Импорт и регистрация

```javascript
// Автоматическая регистрация через index.js
import 'packs/libs/modia';

// Ручное получение экземпляра
const component = $('form[data-component="validation"]').data('modia-component');
```

---

## 🏗 Класс ValidationComponent

### Наследование

```javascript
import { BaseComponent } from '../core.js';

export class ValidationComponent extends BaseComponent {
  // ...
}
```

### Статические свойства

| Свойство | Тип | Значение | Описание |
|----------|-----|----------|----------|
| `componentName` | `string` | `'validation'` | Имя компонента для регистрации |
| `defaultConfig` | `Object` | `{ ... }` | Конфигурация по умолчанию |

### defaultConfig

```javascript
static defaultConfig = {
  live: false,              // Live-валидация при blur
  debounceDelay: 300,       // Задержка debounce (мс)
  validateOnSubmit: true,   // Валидировать при submit формы
  validateOnClick: true,    // Валидировать при клике на [data-validate]
  errorStyle: 'bootstrap'   // Стиль ошибок: rails | bootstrap | custom
};
```

---

## 🔧 Конструктор

```javascript
constructor(element: HTMLElement)
```

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| `element` | `HTMLElement` | ✅ Да | Форма или контейнер с `data-component="validation"` |

**Поведение:**
1. Вызывает `super(element)` (BaseComponent)
2. Объединяет конфиги через `$.extend()`
3. Инициализирует `this.fieldValidators = []`
4. Сохраняет ссылку: `this.$el.data('modia-component', this)`
5. Вызывает `this._init()`

---

## 📋 Публичные методы

### validate()

Валидирует все поля формы.

```javascript
validate(): boolean
```

**Возвращает:** `boolean` — `true` если все поля валидны, иначе `false`

**Пример:**
```javascript
const component = $('form').data('modia-component');
if (component.validate()) {
  console.log('✅ Форма валидна');
} else {
  console.log('❌ Есть ошибки');
}
```

---

### clearErrors()

Очищает все ошибки валидации.

```javascript
clearErrors(): void
```

**Пример:**
```javascript
component.clearErrors();  // Удаляет все сообщения об ошибках
```

---

### refreshRules()

Обновляет правила для всех валидаторов. Используется после динамического изменения полей.

```javascript
refreshRules(): void
```

**Пример:**
```javascript
// После добавления нового поля
$('#add-field').on('click', function() {
  $('#fields-container').append('<input name="tags[]" required>');
  component.refreshRules();  // Обновляет кэш правил
});
```

---

### setErrorStyle(styleName)

Устанавливает стиль ошибок для всех валидаторов.

```javascript
setErrorStyle(styleName: string): void
```

| Параметр | Тип | Описание |
|----------|-----|----------|
| `styleName` | `string` | `'rails'` \| `'bootstrap'` \| `'custom'` |

**Пример:**
```javascript
component.setErrorStyle('rails');  // Переключает на Rails-стиль
```

---

### getFormData()

Сериализует форму в объект. Поддерживает массивы полей (`name[]`).

```javascript
getFormData(): Object|null
```

**Возвращает:** `Object` с данными формы или `null` если элемент не форма

**Пример:**
```javascript
const data = component.getFormData();
// { name: 'Иван', email: 'ivan@example.com', tags: ['tag1', 'tag2'] }
```

**Особенности:**
- Поля с `name='field[]'` возвращаются как массив
- Пустые поля возвращают пустую строку `''`
- Для `div` контейнеров возвращает `null`

---

### getErrors()

Получает все ошибки валидации.

```javascript
getErrors(): Array<{ field: string, message: string }>
```

**Возвращает:** Массив объектов с ошибками

**Пример:**
```javascript
const errors = component.getErrors();
// [
//   { field: 'email', message: 'Некорректный email' },
//   { field: 'name', message: 'Обязательное поле' }
// ]
```

---

### destroy()

Уничтожает компонент, очищает обработчики событий и данные.

```javascript
destroy(): void
```

**Поведение:**
1. Очищает live-валидацию
2. Очищает `this.fieldValidators`
3. Вызывает `super.destroy()` (очистка событий `.modia`)
4. Удаляет данные: `this.$el.removeData('modia-component')`

**Пример:**
```javascript
component.destroy();  // Полная очистка компонента
```

---

## 🔔 События

### Входящие (подписывается компонент)

| Событие | Селектор | Обработчик |
|---------|----------|------------|
| `submit.modia` | `this.$el` (если `form`) | `_handleSubmit()` |
| `click.modia` | `[data-validate]` | `_handleValidateClick()` |
| `validation:field-added.modia` | `this.$el` | `_handleFieldAdded()` |

### Исходящие (триггерит компонент)

| Событие | Данные | Когда | Можно отменить |
|---------|--------|-------|----------------|
| `validation:beforeValidate` | — | Перед валидацией | ✅ `event.preventDefault()` |
| `validation:validated` | `{ isValid: boolean }` | После валидации | ❌ |
| `validation:valid` | — | Если все поля валидны | ❌ |
| `validation:invalid` | `{ errors: [...] }` | Если есть ошибки | ❌ |
| `validation:manual-check` | `{ isValid: boolean }` | При клике на `[data-validate]` | ❌ |

---

## 🔌 Зависимости

| Модуль | Тип | Назначение |
|--------|-----|------------|
| `BaseComponent` | Наследование | Базовый класс, конфигурация, события |
| `FieldValidator` | Сервис | Валидация поля |
| `FieldErrorRenderer` | Сервис | Рендеринг ошибок |
| `validationRules` | Конфигурация | Правила валидации |
| `debounce` | Утилита | Задержка для live-валидации |
| `jQuery` | Библиотека | DOM, события, утилиты |
| `logger` | Сервис | Логирование |

---

## ⚠️ Крайние случаи

| Сценарий | Поведение |
|----------|-----------|
| Массив полей (`name='tags[]'`) | `getFieldValue()` → `Array<string>`, валидация «хотя бы одно заполнено» |
| Скрытое поле + внешний экран | `$valueSource` скрыт, `$errorScreen` видим |
| Contenteditable | `.text()` для получения значения (без HTML) |
| Отключённое поле | `isVisibleForValidation()` → `false`, пропускается |
| Мультиселект | `getFieldValue()` → `Array<string>` |
| Пустая форма (нет полей) | `validate()` → `true` (нет полей = нет ошибок) |

---

## 📚 Связанная документация

| Файл | Назначение |
|------|------------|
| [`docs/component/validation.md`](../../docs/component/validation.md) | User Guide для пользователей |
| [`docs/service/field-validator.md`](../service/field-validator.md) | Сервис валидации поля |
| [`docs/service/field-error-renderer.md`](../service/field-error-renderer.md) | Сервис рендеринга ошибок |
| [`docs/configurations/validation-rules.md`](../configurations/validation-rules.md) | Правила валидации |

---

**Последнее обновление:** 2026-02-25  
**Версия документации:** 1.2.0