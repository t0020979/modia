# 🛠 Contributing to Modia

Этот документ — для тех, кто развивает фреймворк Modia, а не только использует его.

Он фиксирует внутренние соглашения, стратегии и архитектурные решения, которые не видны конечному пользователю, но определяют устойчивость и расширяемость системы.

---

## 🎯 Философия расширения

- **Новые режимы** (`live`, `submit`, `blur`) — не через новые компоненты, а через мета-конфигурацию (например, `data-component-validation="live"`).
- **Новые правила** — добавляются в массив, не переписывая ядро.
- **Новые источники ошибок** — встраиваются в иерархию приоритетов, а не дублируют логику.
- **Новые обработчики** — регистрируются динамически, без рефакторинга оркестратора.
- **Поддержка параметров** — через формат `__KEY__` в шаблонах, регистр не важен.
- **Стили ошибок** — через конфигурацию, не через дублирование рендерера.

**Цель:** расширять без переписывания.

---

## 📁 Структура и именование

```
modia/
└── modia/
    ├── core.js                 ← BaseComponent, Container, ComponentScanner
    ├── index.js                ← регистрация + запуск (точка входа)
    ├── components/             ← компоненты (validation-component.js → class ValidationComponent)
    ├── configurations/         ← данные (validation-rules.js → export const validationRules)
    └── services/               ← логика (field-error-renderer.js → export class FieldErrorRenderer)
```

### Файлы: `kebab-case`

Все файлы проекта используют `kebab-case`:
- `field-validator.js`
- `validation-rules.js`
- `prepare-step-workflow.md`

**Почему `kebab-case`:**
- Совместимость с путями в браузере (некоторые серверы ломают `_` в URL)
- Единый стандарт экосистемы JS (React, Vue, Svelte)
- Читаемость: `field-error-renderer.js` визуально чётче, чем `field_error_renderer.js`
- Согласованность с атрибутами: `data-component-*` (kebab) → файлы в том же стиле

### Классы: `PascalCase`

```javascript
class FieldValidator { ... }
class ValidationComponent { ... }
```

### Переменные и методы: `camelCase`

```javascript
const fieldValue = '';
function validateField() { ... }
```

### Точка входа

- **Основная:** `index.js`
- **Файл `modia.js` в корне не используется и удаляется**
- Регистрация компонентов: только в `index.js`

---

## 📜 Архитектурные решения (ADR)

| Решение | Альтернатива | Почему выбрано |
|---------|--------------|----------------|
| `ComponentScanner` остаётся в `core.js` | Вынести в `scanner.js` | Это часть ядра, а не отдельный компонент |
| Нет `FieldComponent` | Компонент на каждое поле | Нарушает принцип: «валидация — поведение формы» |
| `FieldErrorRenderer` как отдельный сервис | `ValidationContext` внутри компонента | Чёткое разделение ответственности, расширяемость |
| Live-валидация через `data-component-validation="live"` | Отдельный компонент `LiveValidation` | Избегаем дублирования логики и правил |
| Отказ от `modia.js` в корне | Оставить для совместимости | Не нужен при прямом импорте `index.js` |

> 📌 **Полная история ADR:** см. папку `docs/adr/`

---

## ✅ Чек-лист проектирования компонента

Перед созданием компонента задай себе:

- [ ] Это **поведение формы** или **поведение поля**?  
  → Форма = компонент, поле = атрибут.
- [ ] Нужна ли **live-поддержка**?  
  → Реализуй через `data-component-{name}="live"`, не создавай отдельный компонент.
- [ ] Есть ли зависимость от **DOM-шаблонов**?  
  → Укажи источник и поддержи fallback.
- [ ] Можно ли вынести **рендеринг/очистку** в `*Renderer`?  
  → Да — делай. Это упростит поддержку.
- [ ] Сохраняется ли **совместимость с Rails UJS**?  
  → Обязательно: `preventDefault()` только при ошибках.
- [ ] Какие старые атрибуты нужно поддерживать?
  → Укажи в контракте.
- [ ] Какая иерархия сообщений об ошибке?
  → Укажи все 5 уровней.
- [ ] Нужна ли поддержка параметров в шаблонах?
  → Укажи формат `__KEY__`.

---

## 🧩 Ответственности (для разработчика)

### Текущее ядро (v1.0 — реализовано)

| Модуль | Ответственность |
|--------|-----------------|
| `FieldErrorRenderer` | Только отображение и очистка ошибок. Поддерживает иерархию источников. |
| `FieldValidator` | Валидация одного поля: очистка → применение правил → рендер ошибки. |
| `ValidationComponent` | Оркестратор: создаёт валидаторы, управляет режимами, вызывает при событиях. |

### Текущее ядро (v1.1 — реализовано)

| Модуль | Ответственность |
|--------|-----------------|
| `BaseComponent` | Базовый класс: конфигурация, изоляция, `onStateChange` |
| `Container` | Глобальное состояние → рассылка всем компонентам |
| `ComponentScanner` | Авто-инициализация по `data-component` |
| `ValidationComponent` | Валидация формы (submit + live), через `FieldValidator` и `FieldErrorRenderer` |
| `FieldValidator` | Валидация одного поля/группы полей по правилам |
| `FieldErrorRenderer` | Рендеринг и очистка ошибок с поддержкой 5 уровней иерархии |
| `validationRules` | Конфигурация правил валидации |

✅ Работает в legacy-проектах, совместим с Rails, Turbolinks, UJS.

---

## 📋 Иерархия источников ошибок (приоритет)

Приоритет сообщений об ошибке (5 уровней):

1. **Inline-атрибут:** `data-{ruleName}-text-error="..."`
2. **Шаблон в контейнере:** `.error-templates [data-rule="..."]`
3. **Отдельные шаблоны:** `#{templateId}` (например, `#max_length_error_span`)
4. **Сообщение из правила:** `rule.defaultMessage`
5. **Fallback:** только для отладки

---

## 🐛 Debug-режимы (v1.2+)

| Режим | GET-параметр | Data-атрибут | Назначение |
|-------|--------------|--------------|------------|
| Console | `?modia-log` | `data-modia-log` | Логирование в консоль |
| Debug | `?modia-debug` | `data-modia-debug` | Debug режим |
| Diagnostics | `?modia-diag` | `data-modia-diag` | Диагностика (v1.3+) |
| Panel | `?modia-panel` | `data-modia-panel` | UI панель (v1.3+) |

Приоритет: GET-параметры > Data-атрибуты

---

## 🚫 Принципы, запрещающие технический долг

- **Не смешивать ответственности:** рендер ≠ логика ≠ события.
- **Даже "временные" решения** должны соответствовать архитектуре — лучше сразу сделать правильно.
- **Новые правила** — только через добавление в массив, без изменения `ValidationComponent`.
- **Новые источники ошибок** — только через расширение `FieldErrorRenderer`, не через дублирование.
- **Совместимость с legacy** — не ломать DOM, не мешать UJS, поддерживать существующие шаблоны.
- **Названия методов** — избегать слова "legacy", использовать нейтральные термины (`separate`, `container`).
- **Поддержка параметров** — регистр не важен, использовать `toLowerCase()` для ключей.

---

## 💡 Назначение документа

> Этот документ — не для пользователя, а для тебя через 6 месяцев.
>
> Он сохраняет контекст архитектурных решений, чтобы не пришлось заново обсуждать:
> - «А почему мы не сделали FieldComponent?»
> - «А почему Container не используется в ValidationComponent?»

---

## 🚧 Ближайшие архитектурные расширения (v1.1 — в работе)

### 1. Глобальный Event Bus

**Почему:** `Container.setState()` слишком грубый — не позволяет передавать данные, только состояние.

**Решение:** добавить `EventBus` как singleton.

```javascript
EventBus.emit('form:validated', { isValid: false, errors: [...] });
EventBus.on('modal:opened', callback);
```

**Совместимость:** остаётся опциональным — компоненты могут использовать `Container`, `EventBus` или оба.

### 2. Поддержка вложенных компонентов

**Проблема:** сейчас все компоненты — равноправные, нет иерархии.

**Решение:**
- При сканировании: рекурсивно инициализировать внутри корня компонента.
- Ввести локальный контейнер (`LocalContainer`) для дочерних компонентов.
- Поддержка: `data-component-scope="local"` → компонент создаёт свой `Container`.

**Пример:**
```html
<div data-component="modal">
  <form data-component="validation">...</form> <!-- дочерний -->
</div>
```
→ `ValidationComponent` получает локальный state от `ModalComponent`, а не глобальный.

### 3. Расширение правил валидации

- `maxLength` — проверка максимальной длины
- `format` — проверка по регулярному выражению
- `ajax` — асинхронная валидация
- `custom` — кастомные валидаторы

---

## 🧩 Запланированные компоненты

> 📌 **Полный список компонентов с приоритетами:** см. [ROADMAP.md](./ROADMAP.md#-запланированные-компоненты-по-приоритету)

| Компонент | Назначение | Состояние |
|-----------|------------|-----------|
| FormStateComponent | Переключение режимов (regular/cross) через Container | v1.2 |
| ModalComponent | Открытие/закрытие, сканирование внутреннего DOM, локальный контейнер | v1.2 |
| DynamicSelectComponent | Асинхронная подгрузка опций в `<select>` | v1.3 |
| DependentFieldComponent | Блокировка/очистка полей при изменении другого | v1.3 |
| TabGroupComponent | Переключение вкладок с поддержкой `data-component` внутри | v1.4 |
| AccordionComponent | Сворачиваемые секции с сохранением состояния | v1.4 |
| CounterComponent | Счётчик длины, выбранного и т.п. | v1.5 |
| FormLockerComponent | Блокировка после отправки (`data-disable-with`) | v1.5 |

**Все компоненты следуют принципу:** поведение контейнера, а не поля.

---

## 📦 Архитектурные слои (на горизонте)

| Слой | Описание |
|------|----------|
| Core Layer | `BaseComponent`, `Container`, `EventBus`, `ComponentScanner` |
| Service Layer | `FieldValidator`, `FieldErrorRenderer`, `StateResolver` |
| Component Layer | `ValidationComponent`, `ModalComponent`, и др. |
| Configuration Layer | `validationRules.js`, `formStateMap.js`, `templateSources.js` |
| Integration Layer | Совместимость: Turbolinks, UJS, Rails i18n, Webpacker |

---

## 🔄 Версионная стратегия

- **v1.x** — базовая архитектура + компоненты, без breaking changes.
- **v2.0** — возможна:
  - поддержка вложенных контейнеров,
  - унификация источников ошибок через `TemplateRegistry`,
  - опциональный отказ от jQuery (чистый DOM API).

**Принцип:** даже временные решения в v1.x должны соответствовать архитектуре — чтобы v2.0 не требовал переписывания.

---

**Последнее обновление:** 2026-02-16
