# 🛠 Contributing to Modia

**Версия:** 1.2.0  
**Последнее обновление:** 2026-02-17

> Этот документ — для тех, кто развивает фреймворк Modia, а не только использует его.

> Он фиксирует внутренние соглашения, стратегии и архитектурные решения, которые не видны конечному пользователю, но определяют устойчивость и расширяемость системы.

## 🎯 Философия расширения

- **Новые режимы** (`live`, `submit`, `blur`) — не через новые компоненты, а через мета-конфигурацию (например, `data-component-validation="live"`).
- **Новые правила** — добавляются в массив, не переписывая ядро.
- **Новые источники ошибок** — встраиваются в иерархию приоритетов, а не дублируют логику.
- **Новые обработчики** — регистрируются динамически, без рефакторинга оркестратора.
- **Поддержка параметров** — через формат `__KEY__` в шаблонах, регистр не важен.
- **Стили ошибок** — через конфигурацию, не через дублирование рендерера.

**Цель:** расширять без переписывания.

--- 
---

## 🧩 Ответственности (для разработчика)

### Текущее ядро (v1.2 — ✅ реализовано)

| Модуль | Ответственность |
|--------|----------------|
| BaseComponent | Базовый класс: конфигурация, изоляция, onStateChange |
| Container | Глобальное состояние → рассылка всем компонентам |
| ComponentScanner | Авто-инициализация по `data-component` |
| ValidationComponent | Валидация формы (submit) либо контейнер div, через FieldValidator и FieldErrorRenderer |
| FieldValidator | Валидация одного поля/группы полей по правилам |
| FieldErrorRenderer | Рендеринг и очистка ошибок с поддержкой 5 уровней иерархии |
| validationRules | Конфигурация правил валидации |
| Logger Service | Единый сервис логирования (режимы: log, debug, diag, panel) |
| DebugComponent | Управление режимами отладки (1.3) |
| jQuery Data API | Хранение ссылок на компоненты через `$.data('modia-component')` |
| Custom Events | События `modia:initialized`, `modia:scanned`, `modia:component-created` |

✅ Работает в legacy-проектах, совместим с Rails, Turbolinks, UJS.

## 📁 Структура и именование

```
modia/
├── core.js                 ← BaseComponent, Container, ComponentScanner
├── index.js                ← регистрация + запуск (точка входа)
├── components/             ← компоненты (validation.js → class ValidationComponent)
├── services/               ← логика (logger.js → export class Logger)
└── configurations/         ← данные (validation-rules.js → export const validationRules)
```
### Файлы: `kebab-case`

Все файлы проекта используют `kebab-case`:
- `field-validator.js`
- `validation-rules.js`
- `prepare-step-workflow.md`

**Почему `kebab-case`:**
- Совместимость с путями в браузере (некоторые серверы ломают `_` в URL)
- Единый стандарт экосистемы JS (React, Vue, Svelte)
- Читаемость: `field-error-renderer.js` визуально чётче, чем `field_error_renderer.js`
- Согласованность с атрибутами: `data-component-*` (kebab) → файлы в том же стиле

### Классы: `PascalCase`

```javascript
class FieldValidator { ... }
class ValidationComponent { ... }
```

### Переменные и методы: `camelCase`

```javascript
const fieldValue = '';
function validateField() { ... }
```

---

## 🚫 Принципы, запрещающие технический долг

- **Не смешивать ответственности:** рендер ≠ логика ≠ события.
- **Даже "временные" решения** должны соответствовать архитектуре — лучше сразу сделать правильно.
- **Новые правила** — только через добавление в массив, без изменения `ValidationComponent`.
- **Новые источники ошибок** — только через расширение `FieldErrorRenderer`, не через дублирование.
- **Совместимость с legacy** — не ломать DOM, не мешать UJS, поддерживать существующие шаблоны.
- **Названия методов** — избегать слова "legacy", использовать нейтральные термины (`separate`, `container`).
- **Поддержка параметров** — регистр не важен, использовать `toLowerCase()` для ключей.---

## 🧩 Запланированные компоненты

> 📌 **Полный список компонентов с приоритетами:** см. [ROADMAP.md](./ROADMAP.md#-запланированные-компоненты-по-приоритету)

| Компонент | Назначение | Состояние |
|-----------|------------|-----------|
| FormStateComponent | Переключение режимов (regular/cross) через Container | v1.2+ |
| ModalComponent | Открытие/закрытие, сканирование внутреннего DOM, локальный контейнер | v1.2+ |
| DynamicSelectComponent | Асинхронная подгрузка опций в `<select>` | v1.3+ |
| DependentFieldComponent | Блокировка/очистка полей при изменении другого | v1.3+ |
| TabGroupComponent | Переключение вкладок с поддержкой `data-component` внутри | v1.4+ |
| AccordionComponent | Сворачиваемые секции с сохранением состояния | v1.4+ |
| CounterComponent | Счётчик длины, выбранного и т.п. | v1.5+ |
| FormLockerComponent | Блокировка после отправки (`data-disable-with`) | v1.5+ |

**Все компоненты следуют принципу:** поведение контейнера, а не поля.

---

## 📦 Архитектурные слои (на горизонте)

| Слой | Описание |
|------|----------|
| Core Layer | `BaseComponent`, `Container`, `EventBus`, `ComponentScanner` |
| Service Layer | `FieldValidator`, `FieldErrorRenderer`, `StateResolver` |
| Component Layer | `ValidationComponent`, `ModalComponent`, и др. |
| Configuration Layer | `validationRules.js`, `formStateMap.js`, `templateSources.js` |
| Integration Layer | Совместимость: Turbolinks, UJS, Rails i18n, Webpacker |

---

## 🔄 Версионная стратегия

- **v1.x** — базовая архитектура + компоненты, без breaking changes
- **v2.0** — возможна: поддержка вложенных контейнеров, опциональный отказ от jQuery
  - поддержка вложенных контейнеров,
  - унификация источников ошибок через `TemplateRegistry`,
  - опциональный отказ от jQuery (чистый DOM API).

**Принцип:** даже временные решения в v1.x должны соответствовать архитектуре — чтобы v2.0 не требовал переписывания.

## 🚧 Ближайшие архитектурные расширения (v1.3 — план)

1. **EventBus** — глобальная шина событий
2. **LocalContainer** — локальные контейнеры для вложенных компонентов
3. **Рекурсивное сканирование** — инициализация внутри компонентов
4. **Turbolinks поддержка** — полная интеграция

## 🎨 CSS-архитектура (v1.3+ — план)

| Принцип | Описание |
|---------|----------|
| Один основной файл | `modia/modia.css` — функциональные стили |
| Bootstrap 4 | Рекомендуемая зависимость (не строгая) |
| Debug-панель | Стили вынести в отдельный файл при выносе компонента |
| Примеры | Используют Bootstrap 4 CDN + минимальный `examples.css` |
| Встроенный HTML | Вынести в шаблоны (не в JS) |


## 🔒 ENV блокировка логов (v1.3+ — план)

```javascript
window.MODIA_LOG_ENABLED = false;  // Полная блокировка
window.MODIA_LOG_LEVEL = 'error';  // Только ошибки
```

**Приоритет:** ENV > GET-параметры > Data-атрибуты


## 🔧 Распространённые ошибки при интеграции

### jQuery не найден
Убедитесь, что jQuery экспортирован в `window` до импорта Modia:
```js
import $ from 'jquery';
window.$ = window.jQuery = $;  // ← Один оператор!
import 'packs/libs/modia';
```

### Динамические атрибуты
Если атрибуты валидации (`required`, `data-format`) добавляются после инициализации Modia:
```js
// После добавления атрибута — обновите правила
validator.loadRules();
// Или для всей формы:
component.refreshRules();

**Последнее обновление:** 2026-02-25
