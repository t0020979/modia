
# 🧩 Modia — компонентный мини-фреймворк для динамического UI

> Лёгкая система на чистом JavaScript для управления состоянием, компонентами и автоматической инициализацией из DOM.  
> **Без внешних зависимостей. Совместим с Rails, jQuery, Webpacker.**

---

## 🚀 Быстрый старт

1. **Подключите в `application.js`:**
   ```js
   import './modia';
   ```

2. **Добавьте компонент в HTML:**
   ```html
   <div data-component="validation">
     <input required data-max-length="600">
     <button type="submit" data-validate>Сохранить</button>
     
     <!-- Шаблоны ошибок (обязательно внутри контейнера!) -->
     <div class="invisible error-template" id="error_span">
       <span class="text-danger">Обязательное поле</span>
     </div>
   </div>
   ```

3. **Готово!** Валидация запустится автоматически при отправке формы.

---

## 🧱 Ключевые принципы

- **Автоинициализация**: компоненты создаются при наличии `data-component`.
- **Декларативная конфигурация**: через `data-component-*` атрибуты.
- **Изоляция**: каждый компонент работает только внутри своего корня.
- **Расширяемость**: новый компонент = один файл + регистрация.
- **Поддержка динамики**: `Modia.scan(container)` для AJAX/Turbolinks.

---

## 📂 Архитектура

```
frontend/packs/
├── modia.js                 ← точка входа
└── modia/
    ├── core.js              ← BaseComponent, Container, ComponentScanner
    ├── index.js             ← регистрация и автоинициализация
    ├── components/          ← компоненты (validation.js, ...)
    └── configurations/      ← конфигурации (правила валидации, ...)
```

---

## 📚 Документация

- [Контракт компонента](docs/component.md)
- [Компонент валидации](docs/validation.md)

---

## 💡 Примеры использования

### Валидация
Автоматически проверяет `required` и `data-max-length`, отображает ошибки из шаблонов.

### Сканирование после AJAX
```js
$.get('/form', html => {
  $('#modal').html(html);
  Modia.scan($('#modal')[0]);
});
```

---

## ✅ Преимущества

- **Одна строка подключения**
- **Нет глобального состояния**
- **Работает в legacy-проектах**
- **Полный контроль над HTML и стилями**
- **Ленивая инициализация — только там, где нужно**

---

> **Modia** — от *modular* + *via*  

💡 Этот фреймворк — шаг к системной организации JS в legacy-проектах **без миграции на React/Vue**.
