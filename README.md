# 🗺️ ROADMAP — План развития фреймворка Modia

Этот документ описывает эволюцию Modia как системы:
- текущие возможности,
- запланированные архитектурные слои,
- компоненты к реализации,
- поддержку иерархии и событий.

Он не дублирует README, а служит дорожной картой для разработчика фреймворка.

---

## 📖 Оглавление
- [Текущее ядро (v1.0)](#-текущее-ядро-v10---реализовано)
- [Архитектурные расширения (v1.1)](#-архитектурные-расширения-v11---в-работе)
- [Запланированные компоненты](#-запланированные-компоненты-по-приоритету)
- [Архитектурные слои](#-архитектурные-слои-на-горизонте)
- [Версионная стратегия](#-версионная-стратегия)
- [Принципы развития](#-принципы-развития)
- [История релизов](#-история-релизов)

---

## 🧱 Текущее ядро (v1.0 — ✅ реализовано)

| Модуль | Ответственность | Статус |
|--------|-----------------|--------|
| `BaseComponent` | Базовый класс: конфигурация, изоляция, `onStateChange` | ✅ v1.0 |
| `Container` | Глобальное состояние → рассылка всем компонентам | ✅ v1.0 |
| `ComponentScanner` | Авто-инициализация по `data-component` | ✅ v1.0 |
| `ValidationComponent` | Валидация формы (submit + live), через `FieldValidator` и `FieldErrorRenderer` | ✅ v1.0 |
| `FieldValidator` | Валидация одного поля/группы полей по правилам | ✅ v1.0 |
| `FieldErrorRenderer` | Рендеринг и очистка ошибок с поддержкой 5 уровней иерархии | ✅ v1.0 |
| `validationRules` | Конфигурация правил валидации | ✅ v1.0 |

✅ **Работает в legacy-проектах, совместим с Rails, Turbolinks, UJS.**

---

## 🚧 Архитектурные расширения (v1.1 — 🔄 в работе)

### 1. Глобальный Event Bus

| Параметр | Значение |
|----------|----------|
| **Статус** | 🔄 в работе |
| **Цель** | Q1 2026 |
| **Зависимости** | `Container`, `BaseComponent` |

**Почему:** `Container.setState()` слишком грубый — не позволяет передавать данные, только состояние.

**Решение:** добавить `EventBus` как singleton.

```javascript
EventBus.emit('form:validated', { isValid: false, errors: [...] });
EventBus.on('modal:opened', callback);
```

**Совместимость:** остаётся опциональным — компоненты могут использовать `Container`, `EventBus` или оба.

---

### 2. Поддержка вложенных компонентов

| Параметр | Значение |
|----------|----------|
| **Статус** | 🔄 в работе |
| **Цель** | Q1 2026 |
| **Зависимости** | `Container`, `ComponentScanner` |

**Проблема:** сейчас все компоненты — равноправные, нет иерархии.

**Решение:**
- При сканировании: рекурсивно инициализировать внутри корня компонента.
- Ввести локальный контейнер (`LocalContainer`) для дочерних компонентов.
- Поддержка: `data-component-scope="local"` → компонент создаёт свой `Container`.

**Пример:**
```html
<div data-component="modal">
  <form data-component="validation">...</form> <!-- дочерний -->
</div>
```
→ `ValidationComponent` получает локальный state от `ModalComponent`, а не глобальный.

---

### 3. Расширение правил валидации

| Параметр | Значение |
|----------|----------|
| **Статус** | 🔄 в работе |
| **Цель** | Q1 2026 |
| **Зависимости** | `FieldValidator`, `validationRules` |

- `maxLength` — проверка максимальной длины
- `format` — проверка по регулярному выражению
- `ajax` — асинхронная валидация
- `custom` — кастомные валидаторы

---

## 🧩 Запланированные компоненты (по приоритету)

| Компонент | Назначение | Статус | Зависимости | Цель |
|-----------|------------|--------|-------------|------|
| `FormStateComponent` | Переключение режимов (regular/cross) через Container | ⏳ v1.2 | `Container`, `BaseComponent` | Q2 2026 |
| `ModalComponent` | Открытие/закрытие, сканирование внутреннего DOM, локальный контейнер | ⏳ v1.2 | `ComponentScanner` | Q2 2026 |
| `DynamicSelectComponent` | Асинхронная подгрузка опций в `<select>` | ⏳ v1.3 | `EventBus` | Q3 2026 |
| `DependentFieldComponent` | Блокировка/очистка полей при изменении другого | ⏳ v1.3 | `EventBus`, `Container` | Q3 2026 |
| `TabGroupComponent` | Переключение вкладок с поддержкой `data-component` внутри | ⏳ v1.4 | `Container` | Q4 2026 |
| `AccordionComponent` | Сворачиваемые секции с сохранением состояния | ⏳ v1.4 | `Container` | Q4 2026 |
| `CounterComponent` | Счётчик длины, выбранного и т.п. | ⏳ v1.5 | — | Q1 2027 |
| `FormLockerComponent` | Блокировка после отправки (`data-disable-with`) | ⏳ v1.5 | — | Q1 2027 |

**Все компоненты следуют принципу:** поведение контейнера, а не поля.

### Легенда статусов
| Статус | Значение |
|--------|----------|
| ✅ | Реализовано и протестировано |
| 🔄 | В работе (есть контракт в `contracts/actual/`) |
| ⏳ | Запланировано (контракт в `contracts/next/`) |

---

## 📦 Архитектурные слои (на горизонте)

| Слой | Описание | Статус |
|------|----------|--------|
| **Core Layer** | `BaseComponent`, `Container`, `EventBus`, `ComponentScanner` | ✅ v1.0 |
| **Service Layer** | `FieldValidator`, `FieldErrorRenderer`, `StateResolver` | ✅ v1.0 |
| **Component Layer** | `ValidationComponent`, `ModalComponent`, и др. | 🔄 v1.1-v1.5 |
| **Configuration Layer** | `validationRules.js`, `formStateMap.js`, `templateSources.js` | ✅ v1.0 |
| **Integration Layer** | Совместимость: Turbolinks, UJS, Rails i18n, Webpacker | ✅ v1.0 |

---

## 🔄 Версионная стратегия

| Версия | Описание | Breaking Changes |
|--------|----------|------------------|
| **v1.x** | Базовая архитектура + компоненты | ❌ Нет |
| **v2.0** | Возможные изменения: | ✅ Да |
| | - поддержка вложенных контейнеров | |
| | - унификация источников ошибок через `TemplateRegistry` | |
| | - опциональный отказ от jQuery (чистый DOM API) | |

**Принцип:** даже временные решения в v1.x должны соответствовать архитектуре — чтобы v2.0 не требовал переписывания.

---

## 📅 Принципы развития

| Принцип | Описание |
|---------|----------|
| **Контракт на компонент** | Каждый компонент = контракт: требования, ограничения, критерии |
| **Расширение, не переопределение** | Новые возможности — через расширение, не через переопределение |
| **Совместимость с legacy** | Обязательна: не ломать DOM, не мешать UJS |
| **Тестируемость** | Заложена в архитектуру: чистые функции, изоляция, абстракции |

---

## 🐛 Debug-режимы

Для включения логирования добавьте параметр к URL:

```
https://example.com/form?modia-log
https://example.com/form?modia-debug
```

Или data-атрибут на любом элементе:

```html
<body data-modia-log>
```

### Логирование в коде

```javascript
Modia.log('Сообщение', 'info', 'Context');
Modia.info('Информация', 'Context');
Modia.warn('Предупреждение', 'Context');
Modia.error('Ошибка', 'Context');
Modia.success('Успех', 'Context');
```

⚠️ **Важно:** Логи выводятся только при активном режиме отладки.

---

> **Этот документ — живой.** Обновляется при каждом архитектурном решении.
> 
> **Последнее обновление:** 2026-02-17

